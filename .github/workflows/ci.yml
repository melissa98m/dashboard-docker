name: CI + Deploy (Raspberry Pi)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    name: Lint, format & tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create minimal .env
        run: |
          cp .env.example .env 2>/dev/null || touch .env
          echo "DATABASE_URL=sqlite:////tmp/dashboard.db" >> .env
          echo "NEXT_PUBLIC_API_URL=http://localhost:8000" >> .env

      - name: Build Docker images
        run: make build

      - name: Lint API + Web
        run: make lint-ci

      - name: Format check
        run: make format-check

      - name: Run tests
        run: make test-ci

  deploy:
    needs: test
    # Only on push to main or manual trigger (skipped on PR/fork)
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_USER: ${{ secrets.SSH_USER }}
      SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
      DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate deploy secrets
        run: |
          if [ -z "$SSH_HOST" ]; then echo "::error::Secret SSH_HOST is empty. Add it in Settings → Secrets → Actions"; exit 1; fi
          if [ -z "$SSH_USER" ]; then echo "::error::Secret SSH_USER is empty"; exit 1; fi
          if [ -z "$DEPLOY_PATH" ]; then echo "::error::Secret DEPLOY_PATH is empty"; exit 1; fi
          echo "Secrets OK (host configured)"

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add Pi to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p "$SSH_PORT" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Upload project to Raspberry Pi
        run: |
          rsync -avz --delete -e "ssh -p $SSH_PORT" \
            --exclude='.env' --exclude='.git' --exclude='data' --exclude='backups' \
            --exclude='__pycache__' --exclude='node_modules' --exclude='.next' \
            ./ "$SSH_USER@$SSH_HOST:$DEPLOY_PATH/"

      - name: Deploy via SSH
        run: |
          ssh -p "$SSH_PORT" "$SSH_USER@$SSH_HOST" "set -e && cd $DEPLOY_PATH && [ -f .env ] || cp .env.example .env && export COMPOSE_BAKE=false && export DOCKER_BUILDKIT=0 && docker compose build --pull && docker compose up -d --remove-orphans"
